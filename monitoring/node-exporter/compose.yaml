services:
  node-exporter:
    image: prom/node-exporter:latest
    container_name: node-exporter
    restart: unless-stopped
    networks:
      - monitoring-internal
    command:
      - "--path.rootfs=/host"

    volumes:
      - "/:/host:ro,rslave"
networks:
  monitoring-internal:
    external: true
